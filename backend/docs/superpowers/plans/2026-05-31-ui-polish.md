# UI 打磨实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 7 项 UI/UX 打磨——markdown 渲染、utt_id 引用、细滚动条、转写/画像时间戳、画像倒序、去快答前缀。

**Architecture:** 前端 4 个面板组件（InsightCard, SuggestionCard, TranscriptPanel, ProfilePanel）+ ScrollArea UI 组件 + 后端 ProfileEntryPayload 数据模型扩展 + prompt 微调。改动点小而分散，互相独立。

**Tech Stack:** React 19 + TypeScript + Tailwind CSS + react-markdown + base-ui ScrollArea；Python FastAPI + Pydantic v2。

**Spec:** [`backend/docs/superpowers/specs/2026-05-31-ui-polish-design.md`](../specs/2026-05-31-ui-polish-design.md)

---

## 文件结构

**后端修改：**
- `backend/src/agent/events.py` — ProfileEntryPayload 加 `timestamp`, `source_utt_id`
- `backend/src/agent/orchestrator.py` — emit ProfileUpdated 时传 timestamp 和 source_utt_id
- `backend/main.py:120-128` — HTTP history 的 profile_entries 返回加 timestamp/source_utt_id
- `backend/src/agent/prompts.py` — prompt 指示不要加"快答"前缀
- `backend/tests/agent/test_events_schema.py` — 更新 ProfileEntryPayload round-trip 测试

**前端修改：**
- `frontend/src/types/index.ts` — ProfileEntryItem 加 `timestamp?`, `sourceUttId?`
- `frontend/src/components/ui/scroll-area.tsx` — 滚动条缩窄到 6px + 颜色调淡
- `frontend/src/components/insights/InsightCard.tsx` — react-markdown 渲染 + utt_id 引用 + 快答 strip
- `frontend/src/components/insights/SuggestionCard.tsx` — ReadyCard 用 react-markdown 渲染
- `frontend/src/components/transcript/TranscriptPanel.tsx` — 时间戳 + ScrollArea 替换 overflow-auto
- `frontend/src/components/profile/ProfilePanel.tsx` — 时间戳 + utt_id + 倒序 + ScrollArea

---

## Task 1：Backend — ProfileEntryPayload 扩展 timestamp + source_utt_id

**Files:**
- Modify: `backend/src/agent/events.py:173-176`
- Modify: `backend/src/agent/orchestrator.py:181-190`
- Modify: `backend/main.py:120-128`
- Modify: `backend/tests/agent/test_events_schema.py:28`

- [ ] **Step 1: 更新 test_events_schema.py 的 ProfileUpdated case**

修改 `backend/tests/agent/test_events_schema.py` 第 28 行：

```python
    ProfileUpdated(entries=[ProfileEntryPayload(key="姓名", value="张三", subject="client")]),
```

改为：

```python
    ProfileUpdated(entries=[ProfileEntryPayload(key="姓名", value="张三", subject="client",
                                                 timestamp=12.5, source_utt_id="u1")]),
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
cd backend && uv run pytest tests/agent/test_events_schema.py::test_event_roundtrip_preserves_payload -k "evt5" -v
```

Expected: FAIL — `ProfileEntryPayload` 不接受 `timestamp` 和 `source_utt_id` 参数。

- [ ] **Step 3: 修改 ProfileEntryPayload**

`backend/src/agent/events.py`，把：

```python
class ProfileEntryPayload(BaseModel):
    key: str
    value: str
    subject: str
```

改为：

```python
class ProfileEntryPayload(BaseModel):
    key: str
    value: str
    subject: str
    timestamp: float = 0.0
    source_utt_id: str = ""
```

> `= 0.0` 和 `= ""` 是默认值，保证前端旧事件或测试中不传也能 round-trip（向后兼容）。

- [ ] **Step 4: 修改 orchestrator.py emit ProfileUpdated 时传值**

`backend/src/agent/orchestrator.py`，把 handle_utterance 中 emit ProfileUpdated 块：

```python
                    await self._emit_event(ProfileUpdated(
                        entries=[
                            ProfileEntryPayload(
                                key=e.key, value=e.value, subject=e.subject,
                            ) for e in entries
                        ],
                    ))
```

改为：

```python
                    await self._emit_event(ProfileUpdated(
                        entries=[
                            ProfileEntryPayload(
                                key=e.key, value=e.value, subject=e.subject,
                                timestamp=e.timestamp,
                                source_utt_id=e.source_utt_id,
                            ) for e in entries
                        ],
                    ))
```

- [ ] **Step 5: 修改 HTTP history 端点**

`backend/main.py` 的 `/api/sessions/{session_id}/history` 端点，把 `profile_entries` 返回块：

```python
        "profile_entries": [
            {
                "key": e.key,
                "value": e.value,
                "subject": e.subject,
                "category": e.category or "fact",
            }
            for e in profile_entries
        ],
```

改为：

```python
        "profile_entries": [
            {
                "key": e.key,
                "value": e.value,
                "subject": e.subject,
                "category": e.category or "fact",
                "timestamp": e.timestamp,
                "source_utt_id": e.source_utt_id or "",
            }
            for e in profile_entries
        ],
```

- [ ] **Step 6: 运行后端测试**

```bash
cd backend && uv run pytest tests/agent/test_events_schema.py tests/agent/test_orchestrator_emitter.py tests/api/test_history.py -v
```

Expected: 全部 PASS（约 22 个测试）。

- [ ] **Step 7: 提交**

```bash
git add backend/src/agent/events.py backend/src/agent/orchestrator.py backend/main.py backend/tests/agent/test_events_schema.py
git commit -m "feat(profile): ProfileEntryPayload 加 timestamp + source_utt_id
>
> WebSocket 事件和 HTTP history 同步推送这两个字段。
> 默认值保证向后兼容——旧事件不传也能 round-trip。"
```

---

## Task 2：Frontend — ProfilePanel（类型 + 倒序 + 时间戳 + utt_id + ScrollArea）

**Files:**
- Modify: `frontend/src/types/index.ts:41-46`
- Modify: `frontend/src/components/profile/ProfilePanel.tsx`

- [ ] **Step 1: 修改 ProfileEntryItem 类型**

`frontend/src/types/index.ts`，把：

```ts
export type ProfileEntryItem = {
  key: string;
  value: string;
  subject: string;
  category: ProfileCategory;
};
```

改为：

```ts
export type ProfileEntryItem = {
  key: string;
  value: string;
  subject: string;
  category: ProfileCategory;
  timestamp?: number;
  sourceUttId?: string;
};
```

- [ ] **Step 2: 修改 ProfilePanel — 加 import + ScrollArea 替换 overflow-auto + 倒序 + 时间戳 + utt_id**

`frontend/src/components/profile/ProfilePanel.tsx` 顶部 import 区加：

```tsx
import { ScrollArea } from '@/components/ui/scroll-area'
```

所有 `categoryEntries` 调用加 `.reverse()`。修改 `categoryEntries` 函数定义（约第 113-114 行）：

```tsx
  const categoryEntries = (cat: ProfileCategory) =>
    profile.entries.filter((e) => e.category === cat).reverse()
```

将 `EntryList` 改为支持时间戳和 utt_id 显示（约第 29-42 行）：

```tsx
function EntryList({ entries }: { entries: ProfileEntryItem[] }) {
  if (entries.length === 0) {
    return <p className="text-xs text-ink-muted">暂无数据</p>
  }
  return (
    <div className="space-y-1.5">
      {entries.map((e, i) => (
        <div key={i} className="flex items-start gap-2">
          <span className="w-1 h-1 rounded-full mt-1.5 shrink-0 bg-ink-muted" />
          <div className="min-w-0">
            <span className="text-xs text-ink-primary leading-relaxed">{e.key}: {e.value}</span>
            {(e.timestamp != null || e.sourceUttId) && (
              <span className="block text-[10px] text-ink-muted font-mono mt-0.5">
                {e.timestamp != null ? formatTime(e.timestamp) : ''}
                {e.sourceUttId ? ` · 来源: ${e.sourceUttId}` : ''}
              </span>
            )}
          </div>
        </div>
      ))}
    </div>
  )
}
```

在文件顶部加 `formatTime` 工具函数（在 `CATEGORY_LABELS` 之后）：

```tsx
function formatTime(seconds: number): string {
  const m = Math.floor(seconds / 60)
  const s = Math.floor(seconds % 60)
  return `${m}:${s.toString().padStart(2, '0')}`
}
```

将 ProfilePanel 中的 `overflow-auto` 替换为 `ScrollArea`。找到两处：
- 空状态（约 102 行）：`<div className="flex-1 overflow-auto px-5 py-4 space-y-4">`
- 有数据状态（约 170 行）：`<div className="flex-1 overflow-auto px-5 py-4 space-y-5">`

都改为：

```tsx
<ScrollArea className="flex-1 px-5 py-4">
  <div className="space-y-5">
    {/* 内容 */}
  </div>
</ScrollArea>
```

> 注意：空状态模板中的 `space-y-4` 改为 `space-y-5`，统一间距。所有子模块（`EntryList`、`KeyClaims`、`ConfirmedFacts` 等）保持不变。

- [ ] **Step 3: 运行 tsc 确认类型正确**

```bash
cd frontend && pnpm tsc --noEmit
```

Expected: PASS，无类型错误。

- [ ] **Step 4: 提交**

```bash
git add frontend/src/types/index.ts frontend/src/components/profile/ProfilePanel.tsx
git commit -m "feat(profile): 画像面板加时间戳、utt_id引用、倒序排列、ScrollArea"
```

---

## Task 3：Frontend — 滚动条统一细条

**Files:**
- Modify: `frontend/src/components/ui/scroll-area.tsx:43-44`

- [ ] **Step 1: 修改 scroll-area.tsx 宽度和颜色**

`frontend/src/components/ui/scroll-area.tsx`，找到 ScrollBar 的 className（约 43-44 行）：

```tsx
className={cn(
  "flex touch-none p-px transition-colors select-none data-horizontal:h-2.5 data-horizontal:flex-col data-horizontal:border-t data-horizontal:border-t-transparent data-vertical:h-full data-vertical:w-2.5 data-vertical:border-l data-vertical:border-l-transparent",
  className
)}
```

把 `data-vertical:w-2.5` 改为 `data-vertical:w-1.5`，`data-horizontal:h-2.5` 改为 `data-horizontal:h-1.5`：

```tsx
className={cn(
  "flex touch-none p-px transition-colors select-none data-horizontal:h-1.5 data-horizontal:flex-col data-horizontal:border-t data-horizontal:border-t-transparent data-vertical:h-full data-vertical:w-1.5 data-vertical:border-l data-vertical:border-l-transparent",
  className
)}
```

同时修改 Thumb 的 className（约 49-51 行）：

```tsx
className="relative flex-1 rounded-full bg-border/50"
```

改为带 hover 效果：

```tsx
className="relative flex-1 rounded-full bg-border/50 hover:bg-border/80 transition-colors"
```

- [ ] **Step 2: 运行 tsc**

```bash
cd frontend && pnpm tsc --noEmit
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/ui/scroll-area.tsx
git commit -m "style(scrollbar): 滚动条收窄至 6px,颜色调淡,加 hover 过渡"
```

---

## Task 4：Frontend — TranscriptPanel 时间戳 + ScrollArea

**Files:**
- Modify: `frontend/src/components/transcript/TranscriptPanel.tsx`

- [ ] **Step 1: 加 ScrollArea import + formatTime**

`frontend/src/components/transcript/TranscriptPanel.tsx` 顶部 import 区加：

```tsx
import { ScrollArea } from '@/components/ui/scroll-area'
```

在 `speakerLabel` 定义后面加 `formatTime` 函数：

```tsx
function formatTime(seconds: number): string {
  const m = Math.floor(seconds / 60)
  const s = Math.floor(seconds % 60)
  return `${m}:${s.toString().padStart(2, '0')}`
}
```

- [ ] **Step 2: 替换 overflow-auto 为 ScrollArea**

把第 57 行的：

```tsx
<div ref={scrollRef} onScroll={handleScroll} className="flex-1 overflow-auto px-5 py-4">
```

改为：

```tsx
<ScrollArea className="flex-1 px-5 py-4" scrollRef={scrollRef} onScroll={handleScroll}>
```

对应的关闭 `</div>` 改为 `</ScrollArea>`。

> `ScrollArea` 基于 base-ui，不直接支持 `ref` + `onScroll` prop 透传。实际做法：保留原生 div 做滚动容器，但把 `overflow-auto` 改成 ScrollArea 包裹。考虑到转录面板需要 auto-scroll 到底部的行为，直接在 ScrollArea 的 Viewport 上绑定 ref 和 onScroll。简单起见：保留原生 div 但将样式同步为 InsightStream 的 ScrollArea 一致的外观——即继续用 overflow-auto，但通过 CSS 定制滚动条样式。

> **简化方案：** TranscriptPanel 和 ProfilePanel 不再替换为 ScrollArea 组件（避免 auto-scroll 和 ref 绑定冲突），改为在全局 CSS 中统一滚动条样式。这比改 ScrollArea 组件更简单、影响面更可控。

**改为更简单的方案：** 不在 TranscriptPanel 中引入 ScrollArea。保持原有 `overflow-auto`，改为在 Tailwind 全局样式中定制滚动条。

`frontend/src/index.css`（或全局样式文件）加：

```css
@layer base {
  * {
    scrollbar-width: thin;
    scrollbar-color: hsl(var(--border) / 0.5) transparent;
  }
}
```

> `scrollbar-width: thin` 是标准 CSS 属性（Firefox 原生支持，Chrome 121+ 支持）。对于旧 Chrome，这是渐进增强——旧浏览器回退到默认滚动条。

同时修改 TranscriptPanel 每行加时间戳。把第 66-74 行的转写行渲染块：

```tsx
              return (
                <div key={line.id} className="flex gap-2">
                  <span className={`text-xs font-mono shrink-0 ${spk.color}`}>{spk.label}</span>
                  <p className="text-xs text-ink-secondary leading-relaxed">{line.text}</p>
                </div>
              );
```

改为：

```tsx
              return (
                <div key={line.id} className="flex gap-2">
                  <span className="text-[10px] text-ink-muted font-mono w-10 shrink-0 text-right">
                    {formatTime(line.timestamp)}
                  </span>
                  <span className={`text-xs font-mono shrink-0 ${spk.color}`}>{spk.label}</span>
                  <p className="text-xs text-ink-secondary leading-relaxed">{line.text}</p>
                </div>
              );
```

- [ ] **Step 2: 运行 tsc**

```bash
cd frontend && pnpm tsc --noEmit
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/transcript/TranscriptPanel.tsx
git commit -m "feat(transcript): 每行加 MM:SS 时间戳"
```

---

## Task 5：Frontend — InsightCard/SuggestionCard Markdown + utt_id 引用 + 快答 strip

**Files:**
- Modify: `frontend/src/components/insights/InsightCard.tsx`
- Modify: `frontend/src/components/insights/SuggestionCard.tsx`

- [ ] **Step 1: 创建共享的 MarkdownText 组件**

新建 `frontend/src/components/insights/MarkdownText.tsx`：

```tsx
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

const ALLOWED = new Set([
  'p', 'strong', 'em', 'code', 'ul', 'ol', 'li', 'blockquote', 'a', 'br', 'pre',
])

export default function MarkdownText({ children }: { children: string }) {
  return (
    <ReactMarkdown
      remarkPlugins={[remarkGfm]}
      allowedElements={ALLOWED}
      unwrapDisallowed
      components={{
        p: ({ children }) => <p className="text-sm text-ink-secondary leading-relaxed whitespace-pre-wrap mb-1">{children}</p>,
        strong: ({ children }) => <strong className="font-semibold text-ink-primary">{children}</strong>,
        em: ({ children }) => <em className="italic">{children}</em>,
        code: ({ children }) => <code className="text-xs font-mono bg-bg-tertiary px-1 py-0.5 rounded">{children}</code>,
        ul: ({ children }) => <ul className="list-disc list-inside space-y-0.5 my-1">{children}</ul>,
        ol: ({ children }) => <ol className="list-decimal list-inside space-y-0.5 my-1">{children}</ol>,
        li: ({ children }) => <li className="text-sm text-ink-secondary">{children}</li>,
        blockquote: ({ children }) => <blockquote className="border-l-2 border-border-color pl-3 my-1 text-ink-muted italic">{children}</blockquote>,
        a: ({ href, children }) => <a href={href} className="text-accent underline" target="_blank" rel="noopener noreferrer">{children}</a>,
        pre: ({ children }) => <pre className="text-xs font-mono bg-bg-tertiary p-2 rounded my-1 overflow-x-auto">{children}</pre>,
        br: () => <br />,
      }}
    >
      {children}
    </ReactMarkdown>
  )
}
```

- [ ] **Step 2: 修改 InsightCard — Markdown 渲染 + utt_id 引用 + 快答 strip**

`frontend/src/components/insights/InsightCard.tsx` 完整替换为：

```tsx
import { Sparkles } from 'lucide-react'
import type { Insight } from '@/types'
import MarkdownText from './MarkdownText'

export type InsightCardProps = { insight: Insight }

/** 去掉 AI 可能自发加上的"快答"前缀（prompt 兜底）。 */
function stripQuickReply(text: string): string {
  return text.replace(/^\*{0,2}快答\*{0,2}[：:]\s*/u, '')
}

export default function InsightCard({ insight }: InsightCardProps) {
  return (
    <div className="py-4 border-t border-border-color first:border-t-0">
      <div className="flex items-center gap-2 mb-2">
        <Sparkles className="w-3 h-3 text-accent" />
        <span className="text-xs font-medium text-accent">实时洞察</span>
      </div>
      <MarkdownText>{stripQuickReply(insight.text)}</MarkdownText>
      <span className="text-[10px] text-ink-muted font-mono mt-2 block">
        来源: {insight.uttId}
      </span>
    </div>
  )
}
```

- [ ] **Step 3: 修改 SuggestionCard ReadyCard — Markdown 渲染**

`frontend/src/components/insights/SuggestionCard.tsx`，在 import 区加：

```tsx
import MarkdownText from './MarkdownText'
```

把 ReadyCard（约第 138 行）的：

```tsx
          <div className="text-sm text-ink-secondary leading-relaxed mt-3 whitespace-pre-wrap">
            {suggestion.text ?? '分析结果为空'}
          </div>
```

改为：

```tsx
          <div className="mt-3">
            <MarkdownText>{suggestion.text ?? '分析结果为空'}</MarkdownText>
          </div>
```

- [ ] **Step 4: 运行 tsc + 测试**

```bash
cd frontend && pnpm tsc --noEmit && pnpm test
```

Expected: tsc 无错，8 个 reducer 测试 PASS。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/components/insights/MarkdownText.tsx frontend/src/components/insights/InsightCard.tsx frontend/src/components/insights/SuggestionCard.tsx
git commit -m "feat(insights): Markdown 渲染 + utt_id 引用 + 快答前缀 strip"
```

---

## Task 6：Backend — prompt 去快答前缀

**Files:**
- Modify: `backend/src/agent/prompts.py:116-137`

- [ ] **Step 1: 修改 prompt**

`backend/src/agent/prompts.py`，在 `get_child_system_prompt()` 的返回字符串中，"# 风格" 段之前加一句话。找到 `# 风格` 行（约第 135 行）：

```python
    return """你是律师的实时 AI 助手,旁听律师与客户的法律咨询。受众只有律师,禁用"您"对客户说话,不替律师指导客户。

# 两种工作方式

**快答** —— 你的主要工作。律师在听客户说话的同时,你给他能立刻用上的东西:可能是一句法条提点、一个该追问的事实、一个被忽略的风险、一段速算,或任何你判断此刻对律师有用的洞察。哪怕只是半句话,只要有用就说出来。简短、果断。

**深析**(调 `deep_analysis(topic, rationale)`)—— 律师需要停下来读的结构化产物。会切换工作模式、暂停等律师确认。

判断准绳:律师此刻需要的是「接住即用」还是「停下来读」?你在听对话流,自己判断。

# 允许沉默
你要说的话律师此刻并不需要,就不要说。但记住,一个有用的快答远比沉默更有价值。

# 工具
- `fetch_more_transcript(start, end)`:默认窗口看不到的早期内容确有必要时拉取。
- `deep_analysis(topic, rationale)`:切到深析模式。topic 直白,rationale 让律师能判断此刻是否要切。

# 风格
对律师直接说话,专业紧凑;引法条带编号;计算展示公式;不绕弯子。
"""
```

改为（在 `# 风格` 前加一句）：

```python
    return """你是律师的实时 AI 助手,旁听律师与客户的法律咨询。受众只有律师,禁用"您"对客户说话,不替律师指导客户。

# 两种工作方式

**快答** —— 你的主要工作。律师在听客户说话的同时,你给他能立刻用上的东西:可能是一句法条提点、一个该追问的事实、一个被忽略的风险、一段速算,或任何你判断此刻对律师有用的洞察。哪怕只是半句话,只要有用就说出来。简短、果断。

**深析**(调 `deep_analysis(topic, rationale)`)—— 律师需要停下来读的结构化产物。会切换工作模式、暂停等律师确认。

判断准绳:律师此刻需要的是「接住即用」还是「停下来读」?你在听对话流,自己判断。

# 允许沉默
你要说的话律师此刻并不需要,就不要说。但记住,一个有用的快答远比沉默更有价值。

# 工具
- `fetch_more_transcript(start, end)`:默认窗口看不到的早期内容确有必要时拉取。
- `deep_analysis(topic, rationale)`:切到深析模式。topic 直白,rationale 让律师能判断此刻是否要切。

# 快答格式
不要在你的快答回复中加"快答"标题或任何前缀,直接说出对律师有用的内容即可。

# 风格
对律师直接说话,专业紧凑;引法条带编号;计算展示公式;不绕弯子。
"""
```

- [ ] **Step 2: 提交**

```bash
git add backend/src/agent/prompts.py
git commit -m "fix(prompt): 明确禁止 AI 在快答输出中加"快答"前缀"
```

---

## 验证标准

- [ ] InsightCard 中 markdown 正确渲染（加粗、列表可见）
- [ ] InsightCard 底部显示 `来源: {uttId}`
- [ ] 三个面板滚动条风格统一（`scrollbar-width: thin`）
- [ ] TranscriptPanel 每行显示 `MM:SS` 时间戳
- [ ] ProfilePanel 条目显示时间戳和 utt_id 引用
- [ ] ProfilePanel 条目倒序排列（最新在上）
- [ ] 后端 `test_events_schema.py` 通过（含新字段 round-trip）
- [ ] 前端 `tsc --noEmit` 通过 + 8 个 reducer 测试 PASS
